# C4
#include<stdio.h>
#include<math.h>
int main()
{
    double number,squareroot;
    printf("enter a number:");
    scanf("%IF",&number);
    squareroot=sqrt(number);
    printf("squareroot of %2IF=%2IF",number,squareroot);
    return 0;
}
OUTPUT:
enter a number:265
squareroot of %2IF=%2IF
